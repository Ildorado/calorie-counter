export enum lightModeColors {
  backgroundDefault = "#fff",
  secondary = "#1A76D2",
}
export enum darkModeColors {
  backgroundDefault = "#212121",
  secondary = "#333333",
}
// static colors - colors that don't change with theme
export enum staticColors {
  white = "#fff",
  black = '#000'
}
